// using one key combination comment out the line below

var meanComment = "3 is the average of 3"

// Using one key combination indent the line inside the if statement
// Using one key combination outdent the second line

if ('Bob ross' === 'Cool') {
console.log('best man alive')
        console.log('by far the best')
}

// select these three lines of code and comment them all out

function badIdea() {
    console.log('Bubble Wrap pants')
}

// format the function below one line at a time

function looper() {
for(let i = 0; i < 10; i ++) {
if(i % 2 === 0) {
console.log(i, 'Is even')
}
}
}

// collapse or fold the contents of this function using only one key combination

function distraction() {
    let annoying = "superlongterriblestringtahtdoesathing";
    if(annoying) {
        return true
    }
    else {
        return false
    }
}

// with the cursor inside the string, comment out the line below


var name = 'Thor odinson'

// with the cursor at the opening curly brace, press one key combination to go to the closing curly brace

for (let i = 0; i > 10; i++) {
    if(i === 1) {
        console.log('its a one')
    }
}

// format the function below one line at a time

function thisOrThat(times) {
for (let i = 0; i < 10; i++) {
if(i >= 5) {
console.log('below or is 5')
}
else if(i < 5) {
console.log('above')
}
}
}

// fold the content of the first if statement, but not the second using one key combination

function letMeSee(value) {
    if(value === 'truth') {
        return true
    } else {
        return false
    }
}

// With the cursor selected on the opening curly brace for the arrow function go to the closing brace with one key combination

arrayOfData.forEach((data) => {
    if (data === 'bob') {
        return true
    }
    return false
})

// fold all the content on the page using one key combination
// unfold it

// Format the whole document with one keyboard combination
