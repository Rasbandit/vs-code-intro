// Duplicate the line below using one key combination
// select the new name variable using only one key combination and rename the variable to newGuy
var name = "Bob Ross"

// Move the variable declaration to inside the function using the same key combination twice
var info = 'Super Secret Password'

function isItSecret(param) {
    if (info === param) {
        console.log('Its super secret')
    }
}

// With the cursor positioned inside the string 'margin', make a new line under the sheen variable

var sheen = "martin";
var goat = "da man"

// Only Using the keyboard create 5 cursors and make 5 variables with the same name;

// Code here


// Select all instances of the variable x by pressing the same key combination 4 times
// change the variable to now be called age
function checkAge(x) {
    if(x >= 26) {
        return 'Can rent a car'
    } if (x >= 21) {
        return 'can drink'
    } if (x >= 16 ) {
        return 'can drive'
    }
}

// using only the keyboard use the find tool, select all instances of the word 'never'
// replace it with 'always'
let storyTime = `
never going to give you up,
never going to let you down,
never going to make you cry,
never going to drive away
`

// With your cursor inside the string 'gonna give you up' make a new line above the return

function loyaltyTest () {
    return 'gonna give you up'
}


// select all instances of the word sirName using 3 key combinations
// change the property names to to lastName
// Then select all the single quotes below and turn them into double quotes
var guy = {
    sirName = 'Ross',
    firstName = 'Bob'
}

var guyTwo = {
    sirName = 'Simmons',
    firstName = 'Richard'
}

var guyThree = {
    sirName = 'Faux',
    firstName = 'Guy'
}


// Using the mouse select inside each empty string and make a new cursor for each
// Fill them all with the same string

var badoof = ''
var kardashian = ''
var bob = ''
var pokemon = ''


// using one key combination duplicate the line below
// using two key combination select both instances of the word 'Item'
// replace it with Cart

import Item from './components/Item'

// using one key combination select all the text in the document
// delete it
// undo it